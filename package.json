{
  "name": "opencode-skills-notifier",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "for f in src/*.test.ts; do bun test \"$f\" || exit 1; done"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/node": "^25.5.2",
    "bun-types": "^1.3.11",
    "typescript": "^5"
  }
}
